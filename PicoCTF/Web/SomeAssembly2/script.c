/* Automatically generated by wasm2c */
#include <assert.h>
#include <math.h>
#include <stddef.h>
#include <string.h>
#if defined(_MSC_VER)
#include <intrin.h>
#include <malloc.h>
#define alloca _alloca
#else
#include <alloca.h>
#endif

#include "script.h"

#define TRAP(x) (wasm_rt_trap(WASM_RT_TRAP_##x), 0)

#if WASM_RT_USE_STACK_DEPTH_COUNT
#define FUNC_PROLOGUE                                            \
  if (++wasm_rt_call_stack_depth > WASM_RT_MAX_CALL_STACK_DEPTH) \
    TRAP(EXHAUSTION);

#define FUNC_EPILOGUE --wasm_rt_call_stack_depth
#else
#define FUNC_PROLOGUE

#define FUNC_EPILOGUE
#endif

#define UNREACHABLE TRAP(UNREACHABLE)

#define CALL_INDIRECT(table, t, ft, x, ...)             \
  (LIKELY((x) < table.size && table.data[x].func &&     \
          table.data[x].func_type == func_types[ft]) || \
       TRAP(CALL_INDIRECT),                             \
   ((t)table.data[x].func)(__VA_ARGS__))

#define RANGE_CHECK(mem, offset, len)               \
  if (UNLIKELY(offset + (uint64_t)len > mem->size)) \
    TRAP(OOB);

#if WASM_RT_MEMCHECK_SIGNAL_HANDLER
#define MEMCHECK(mem, a, t)
#else
#define MEMCHECK(mem, a, t) RANGE_CHECK(mem, a, sizeof(t))
#endif

#ifdef __GNUC__
#define wasm_asm __asm__
#else
#define wasm_asm(X)
#endif

#if WABT_BIG_ENDIAN
static inline void load_data(void* dest, const void* src, size_t n) {
  size_t i = 0;
  u8* dest_chars = dest;
  memcpy(dest, src, n);
  for (i = 0; i < (n >> 1); i++) {
    u8 cursor = dest_chars[i];
    dest_chars[i] = dest_chars[n - i - 1];
    dest_chars[n - i - 1] = cursor;
  }
}
#define LOAD_DATA(m, o, i, s)                   \
  do {                                          \
    RANGE_CHECK((&m), m.size - o - s, s);       \
    load_data(&(m.data[m.size - o - s]), i, s); \
  } while (0)
#define DEFINE_LOAD(name, t1, t2, t3)                                  \
  static inline t3 name(wasm_rt_memory_t* mem, u64 addr) {             \
    MEMCHECK(mem, addr, t1);                                           \
    t1 result;                                                         \
    wasm_rt_memcpy(&result, &mem->data[mem->size - addr - sizeof(t1)], \
                   sizeof(t1));                                        \
    wasm_asm("" ::"r"(result));                                        \
    return (t3)(t2)result;                                             \
  }

#define DEFINE_STORE(name, t1, t2)                                      \
  static inline void name(wasm_rt_memory_t* mem, u64 addr, t2 value) {  \
    MEMCHECK(mem, addr, t1);                                            \
    t1 wrapped = (t1)value;                                             \
    wasm_rt_memcpy(&mem->data[mem->size - addr - sizeof(t1)], &wrapped, \
                   sizeof(t1));                                         \
  }
#else
static inline void load_data(void* dest, const void* src, size_t n) {
  memcpy(dest, src, n);
}
#define LOAD_DATA(m, o, i, s)      \
  do {                             \
    RANGE_CHECK((&m), o, s);       \
    load_data(&(m.data[o]), i, s); \
  } while (0)
#define DEFINE_LOAD(name, t1, t2, t3)                      \
  static inline t3 name(wasm_rt_memory_t* mem, u64 addr) { \
    MEMCHECK(mem, addr, t1);                               \
    t1 result;                                             \
    wasm_rt_memcpy(&result, &mem->data[addr], sizeof(t1)); \
    wasm_asm("" ::"r"(result));                            \
    return (t3)(t2)result;                                 \
  }

#define DEFINE_STORE(name, t1, t2)                                     \
  static inline void name(wasm_rt_memory_t* mem, u64 addr, t2 value) { \
    MEMCHECK(mem, addr, t1);                                           \
    t1 wrapped = (t1)value;                                            \
    wasm_rt_memcpy(&mem->data[addr], &wrapped, sizeof(t1));            \
  }
#endif

DEFINE_LOAD(i32_load, u32, u32, u32)
DEFINE_LOAD(i64_load, u64, u64, u64)
DEFINE_LOAD(f32_load, f32, f32, f32)
DEFINE_LOAD(f64_load, f64, f64, f64)
DEFINE_LOAD(i32_load8_s, s8, s32, u32)
DEFINE_LOAD(i64_load8_s, s8, s64, u64)
DEFINE_LOAD(i32_load8_u, u8, u32, u32)
DEFINE_LOAD(i64_load8_u, u8, u64, u64)
DEFINE_LOAD(i32_load16_s, s16, s32, u32)
DEFINE_LOAD(i64_load16_s, s16, s64, u64)
DEFINE_LOAD(i32_load16_u, u16, u32, u32)
DEFINE_LOAD(i64_load16_u, u16, u64, u64)
DEFINE_LOAD(i64_load32_s, s32, s64, u64)
DEFINE_LOAD(i64_load32_u, u32, u64, u64)
DEFINE_STORE(i32_store, u32, u32)
DEFINE_STORE(i64_store, u64, u64)
DEFINE_STORE(f32_store, f32, f32)
DEFINE_STORE(f64_store, f64, f64)
DEFINE_STORE(i32_store8, u8, u32)
DEFINE_STORE(i32_store16, u16, u32)
DEFINE_STORE(i64_store8, u8, u64)
DEFINE_STORE(i64_store16, u16, u64)
DEFINE_STORE(i64_store32, u32, u64)

#if defined(_MSC_VER)

// Adapted from
// https://github.com/nemequ/portable-snippets/blob/master/builtin/builtin.h

static inline int I64_CLZ(unsigned long long v) {
  unsigned long r = 0;
#if defined(_M_AMD64) || defined(_M_ARM)
  if (_BitScanReverse64(&r, v)) {
    return 63 - r;
  }
#else
  if (_BitScanReverse(&r, (unsigned long)(v >> 32))) {
    return 31 - r;
  } else if (_BitScanReverse(&r, (unsigned long)v)) {
    return 63 - r;
  }
#endif
  return 64;
}

static inline int I32_CLZ(unsigned long v) {
  unsigned long r = 0;
  if (_BitScanReverse(&r, v)) {
    return 31 - r;
  }
  return 32;
}

static inline int I64_CTZ(unsigned long long v) {
  if (!v) {
    return 64;
  }
  unsigned long r = 0;
#if defined(_M_AMD64) || defined(_M_ARM)
  _BitScanForward64(&r, v);
  return (int)r;
#else
  if (_BitScanForward(&r, (unsigned int)(v))) {
    return (int)(r);
  }

  _BitScanForward(&r, (unsigned int)(v >> 32));
  return (int)(r + 32);
#endif
}

static inline int I32_CTZ(unsigned long v) {
  if (!v) {
    return 32;
  }
  unsigned long r = 0;
  _BitScanForward(&r, v);
  return (int)r;
}

#define POPCOUNT_DEFINE_PORTABLE(f_n, T)                            \
  static inline u32 f_n(T x) {                                      \
    x = x - ((x >> 1) & (T) ~(T)0 / 3);                             \
    x = (x & (T) ~(T)0 / 15 * 3) + ((x >> 2) & (T) ~(T)0 / 15 * 3); \
    x = (x + (x >> 4)) & (T) ~(T)0 / 255 * 15;                      \
    return (T)(x * ((T) ~(T)0 / 255)) >> (sizeof(T) - 1) * 8;       \
  }

POPCOUNT_DEFINE_PORTABLE(I32_POPCNT, u32)
POPCOUNT_DEFINE_PORTABLE(I64_POPCNT, u64)

#undef POPCOUNT_DEFINE_PORTABLE

#else

#define I32_CLZ(x) ((x) ? __builtin_clz(x) : 32)
#define I64_CLZ(x) ((x) ? __builtin_clzll(x) : 64)
#define I32_CTZ(x) ((x) ? __builtin_ctz(x) : 32)
#define I64_CTZ(x) ((x) ? __builtin_ctzll(x) : 64)
#define I32_POPCNT(x) (__builtin_popcount(x))
#define I64_POPCNT(x) (__builtin_popcountll(x))

#endif

#define DIV_S(ut, min, x, y)                                      \
  ((UNLIKELY((y) == 0))                                           \
       ? TRAP(DIV_BY_ZERO)                                        \
       : (UNLIKELY((x) == min && (y) == -1)) ? TRAP(INT_OVERFLOW) \
                                             : (ut)((x) / (y)))

#define REM_S(ut, min, x, y) \
  ((UNLIKELY((y) == 0))      \
       ? TRAP(DIV_BY_ZERO)   \
       : (UNLIKELY((x) == min && (y) == -1)) ? 0 : (ut)((x) % (y)))

#define I32_DIV_S(x, y) DIV_S(u32, INT32_MIN, (s32)x, (s32)y)
#define I64_DIV_S(x, y) DIV_S(u64, INT64_MIN, (s64)x, (s64)y)
#define I32_REM_S(x, y) REM_S(u32, INT32_MIN, (s32)x, (s32)y)
#define I64_REM_S(x, y) REM_S(u64, INT64_MIN, (s64)x, (s64)y)

#define DIVREM_U(op, x, y) \
  ((UNLIKELY((y) == 0)) ? TRAP(DIV_BY_ZERO) : ((x)op(y)))

#define DIV_U(x, y) DIVREM_U(/, x, y)
#define REM_U(x, y) DIVREM_U(%, x, y)

#define ROTL(x, y, mask) \
  (((x) << ((y) & (mask))) | ((x) >> (((mask) - (y) + 1) & (mask))))
#define ROTR(x, y, mask) \
  (((x) >> ((y) & (mask))) | ((x) << (((mask) - (y) + 1) & (mask))))

#define I32_ROTL(x, y) ROTL(x, y, 31)
#define I64_ROTL(x, y) ROTL(x, y, 63)
#define I32_ROTR(x, y) ROTR(x, y, 31)
#define I64_ROTR(x, y) ROTR(x, y, 63)

#define FMIN(x, y)                                                     \
  ((UNLIKELY((x) != (x)))                                              \
       ? NAN                                                           \
       : (UNLIKELY((y) != (y)))                                        \
             ? NAN                                                     \
             : (UNLIKELY((x) == 0 && (y) == 0)) ? (signbit(x) ? x : y) \
                                                : (x < y) ? x : y)

#define FMAX(x, y)                                                     \
  ((UNLIKELY((x) != (x)))                                              \
       ? NAN                                                           \
       : (UNLIKELY((y) != (y)))                                        \
             ? NAN                                                     \
             : (UNLIKELY((x) == 0 && (y) == 0)) ? (signbit(x) ? y : x) \
                                                : (x > y) ? x : y)

#define TRUNC_S(ut, st, ft, min, minop, max, x)                           \
  ((UNLIKELY((x) != (x)))                                                 \
       ? TRAP(INVALID_CONVERSION)                                         \
       : (UNLIKELY(!((x)minop(min) && (x) < (max)))) ? TRAP(INT_OVERFLOW) \
                                                     : (ut)(st)(x))

#define I32_TRUNC_S_F32(x) \
  TRUNC_S(u32, s32, f32, (f32)INT32_MIN, >=, 2147483648.f, x)
#define I64_TRUNC_S_F32(x) \
  TRUNC_S(u64, s64, f32, (f32)INT64_MIN, >=, (f32)INT64_MAX, x)
#define I32_TRUNC_S_F64(x) \
  TRUNC_S(u32, s32, f64, -2147483649., >, 2147483648., x)
#define I64_TRUNC_S_F64(x) \
  TRUNC_S(u64, s64, f64, (f64)INT64_MIN, >=, (f64)INT64_MAX, x)

#define TRUNC_U(ut, ft, max, x)                                          \
  ((UNLIKELY((x) != (x)))                                                \
       ? TRAP(INVALID_CONVERSION)                                        \
       : (UNLIKELY(!((x) > (ft)-1 && (x) < (max)))) ? TRAP(INT_OVERFLOW) \
                                                    : (ut)(x))

#define I32_TRUNC_U_F32(x) TRUNC_U(u32, f32, 4294967296.f, x)
#define I64_TRUNC_U_F32(x) TRUNC_U(u64, f32, (f32)UINT64_MAX, x)
#define I32_TRUNC_U_F64(x) TRUNC_U(u32, f64, 4294967296., x)
#define I64_TRUNC_U_F64(x) TRUNC_U(u64, f64, (f64)UINT64_MAX, x)

#define TRUNC_SAT_S(ut, st, ft, min, smin, minop, max, smax, x) \
  ((UNLIKELY((x) != (x)))                                       \
       ? 0                                                      \
       : (UNLIKELY(!((x)minop(min))))                           \
             ? smin                                             \
             : (UNLIKELY(!((x) < (max)))) ? smax : (ut)(st)(x))

#define I32_TRUNC_SAT_S_F32(x)                                            \
  TRUNC_SAT_S(u32, s32, f32, (f32)INT32_MIN, INT32_MIN, >=, 2147483648.f, \
              INT32_MAX, x)
#define I64_TRUNC_SAT_S_F32(x)                                              \
  TRUNC_SAT_S(u64, s64, f32, (f32)INT64_MIN, INT64_MIN, >=, (f32)INT64_MAX, \
              INT64_MAX, x)
#define I32_TRUNC_SAT_S_F64(x)                                        \
  TRUNC_SAT_S(u32, s32, f64, -2147483649., INT32_MIN, >, 2147483648., \
              INT32_MAX, x)
#define I64_TRUNC_SAT_S_F64(x)                                              \
  TRUNC_SAT_S(u64, s64, f64, (f64)INT64_MIN, INT64_MIN, >=, (f64)INT64_MAX, \
              INT64_MAX, x)

#define TRUNC_SAT_U(ut, ft, max, smax, x)               \
  ((UNLIKELY((x) != (x))) ? 0                           \
                          : (UNLIKELY(!((x) > (ft)-1))) \
                                ? 0                     \
                                : (UNLIKELY(!((x) < (max)))) ? smax : (ut)(x))

#define I32_TRUNC_SAT_U_F32(x) \
  TRUNC_SAT_U(u32, f32, 4294967296.f, UINT32_MAX, x)
#define I64_TRUNC_SAT_U_F32(x) \
  TRUNC_SAT_U(u64, f32, (f32)UINT64_MAX, UINT64_MAX, x)
#define I32_TRUNC_SAT_U_F64(x) TRUNC_SAT_U(u32, f64, 4294967296., UINT32_MAX, x)
#define I64_TRUNC_SAT_U_F64(x) \
  TRUNC_SAT_U(u64, f64, (f64)UINT64_MAX, UINT64_MAX, x)

#define DEFINE_REINTERPRET(name, t1, t2) \
  static inline t2 name(t1 x) {          \
    t2 result;                           \
    memcpy(&result, &x, sizeof(result)); \
    return result;                       \
  }

DEFINE_REINTERPRET(f32_reinterpret_i32, u32, f32)
DEFINE_REINTERPRET(i32_reinterpret_f32, f32, u32)
DEFINE_REINTERPRET(f64_reinterpret_i64, u64, f64)
DEFINE_REINTERPRET(i64_reinterpret_f64, f64, u64)

static float quiet_nanf(float x) {
  uint32_t tmp;
  memcpy(&tmp, &x, 4);
  tmp |= 0x7fc00000lu;
  memcpy(&x, &tmp, 4);
  return x;
}

static double quiet_nan(double x) {
  uint64_t tmp;
  memcpy(&tmp, &x, 8);
  tmp |= 0x7ff8000000000000llu;
  memcpy(&x, &tmp, 8);
  return x;
}

static double wasm_quiet(double x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nan(x);
  }
  return x;
}

static float wasm_quietf(float x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nanf(x);
  }
  return x;
}

static double wasm_floor(double x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nan(x);
  }
  return floor(x);
}

static float wasm_floorf(float x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nanf(x);
  }
  return floorf(x);
}

static double wasm_ceil(double x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nan(x);
  }
  return ceil(x);
}

static float wasm_ceilf(float x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nanf(x);
  }
  return ceilf(x);
}

static double wasm_trunc(double x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nan(x);
  }
  return trunc(x);
}

static float wasm_truncf(float x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nanf(x);
  }
  return truncf(x);
}

static float wasm_nearbyintf(float x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nanf(x);
  }
  return nearbyintf(x);
}

static double wasm_nearbyint(double x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nan(x);
  }
  return nearbyint(x);
}

static float wasm_fabsf(float x) {
  if (UNLIKELY(isnan(x))) {
    uint32_t tmp;
    memcpy(&tmp, &x, 4);
    tmp = tmp & ~(1 << 31);
    memcpy(&x, &tmp, 4);
    return x;
  }
  return fabsf(x);
}

static double wasm_fabs(double x) {
  if (UNLIKELY(isnan(x))) {
    uint64_t tmp;
    memcpy(&tmp, &x, 8);
    tmp = tmp & ~(1ll << 63);
    memcpy(&x, &tmp, 8);
    return x;
  }
  return fabs(x);
}

static double wasm_sqrt(double x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nan(x);
  }
  return sqrt(x);
}

static float wasm_sqrtf(float x) {
  if (UNLIKELY(isnan(x))) {
    return quiet_nanf(x);
  }
  return sqrtf(x);
}

static inline void memory_fill(wasm_rt_memory_t* mem, u32 d, u32 val, u32 n) {
  RANGE_CHECK(mem, d, n);
  memset(mem->data + d, val, n);
}

static inline void memory_copy(wasm_rt_memory_t* dest,
                               const wasm_rt_memory_t* src,
                               u32 dest_addr,
                               u32 src_addr,
                               u32 n) {
  RANGE_CHECK(dest, dest_addr, n);
  RANGE_CHECK(src, src_addr, n);
  memmove(dest->data + dest_addr, src->data + src_addr, n);
}

static inline void memory_init(wasm_rt_memory_t* dest,
                               const u8* src,
                               u32 src_size,
                               u32 dest_addr,
                               u32 src_addr,
                               u32 n) {
  if (UNLIKELY(src_addr + (uint64_t)n > src_size))
    TRAP(OOB);
  LOAD_DATA((*dest), dest_addr, src + src_addr, n);
}

typedef struct {
  uint32_t func_type_index;
  wasm_rt_function_ptr_t func;
  size_t module_offset;
} wasm_elem_segment_expr_t;

static inline void funcref_table_init(wasm_rt_funcref_table_t* dest,
                                      const wasm_elem_segment_expr_t* src,
                                      u32 src_size,
                                      u32 dest_addr,
                                      u32 src_addr,
                                      u32 n,
                                      void* module_instance,
                                      const u32* func_types) {
  if (UNLIKELY(src_addr + (uint64_t)n > src_size))
    TRAP(OOB);
  if (UNLIKELY(dest_addr + (uint64_t)n > dest->size))
    TRAP(OOB);
  for (u32 i = 0; i < n; i++) {
    const wasm_elem_segment_expr_t* src_expr = &src[src_addr + i];
    dest->data[dest_addr + i] = (wasm_rt_funcref_t){
        func_types[src_expr->func_type_index], src_expr->func,
        (char*)module_instance + src_expr->module_offset};
  }
}

// Currently Wasm only supports initializing externref tables with ref.null.
static inline void externref_table_init(wasm_rt_externref_table_t* dest,
                                        u32 src_size,
                                        u32 dest_addr,
                                        u32 src_addr,
                                        u32 n) {
  if (UNLIKELY(src_addr + (uint64_t)n > src_size))
    TRAP(OOB);
  if (UNLIKELY(dest_addr + (uint64_t)n > dest->size))
    TRAP(OOB);
  for (u32 i = 0; i < n; i++) {
    dest->data[dest_addr + i] = wasm_rt_externref_null_value;
  }
}

#define DEFINE_TABLE_COPY(type)                                              \
  static inline void type##_table_copy(wasm_rt_##type##_table_t* dest,       \
                                       const wasm_rt_##type##_table_t* src,  \
                                       u32 dest_addr, u32 src_addr, u32 n) { \
    if (UNLIKELY(dest_addr + (uint64_t)n > dest->size))                      \
      TRAP(OOB);                                                             \
    if (UNLIKELY(src_addr + (uint64_t)n > src->size))                        \
      TRAP(OOB);                                                             \
                                                                             \
    memmove(dest->data + dest_addr, src->data + src_addr,                    \
            n * sizeof(wasm_rt_##type##_t));                                 \
  }

DEFINE_TABLE_COPY(funcref)
DEFINE_TABLE_COPY(externref)

#define DEFINE_TABLE_GET(type)                        \
  static inline wasm_rt_##type##_t type##_table_get(  \
      const wasm_rt_##type##_table_t* table, u32 i) { \
    if (UNLIKELY(i >= table->size))                   \
      TRAP(OOB);                                      \
    return table->data[i];                            \
  }

DEFINE_TABLE_GET(funcref)
DEFINE_TABLE_GET(externref)

#define DEFINE_TABLE_SET(type)                                               \
  static inline void type##_table_set(const wasm_rt_##type##_table_t* table, \
                                      u32 i, const wasm_rt_##type##_t val) { \
    if (UNLIKELY(i >= table->size))                                          \
      TRAP(OOB);                                                             \
    table->data[i] = val;                                                    \
  }

DEFINE_TABLE_SET(funcref)
DEFINE_TABLE_SET(externref)

#define DEFINE_TABLE_FILL(type)                                               \
  static inline void type##_table_fill(const wasm_rt_##type##_table_t* table, \
                                       u32 d, const wasm_rt_##type##_t val,   \
                                       u32 n) {                               \
    if (UNLIKELY((uint64_t)d + n > table->size))                              \
      TRAP(OOB);                                                              \
    for (uint32_t i = d; i < d + n; i++) {                                    \
      table->data[i] = val;                                                   \
    }                                                                         \
  }

DEFINE_TABLE_FILL(funcref)
DEFINE_TABLE_FILL(externref)

static bool s_module_initialized = false;

static u32 func_types[4];

static void init_func_types(void) {
  func_types[0] = wasm_rt_register_func_type(0, 0);
  func_types[1] = wasm_rt_register_func_type(2, 1, WASM_RT_I32, WASM_RT_I32, WASM_RT_I32);
  func_types[2] = wasm_rt_register_func_type(0, 1, WASM_RT_I32);
  func_types[3] = wasm_rt_register_func_type(2, 0, WASM_RT_I32, WASM_RT_I32);
}

static void w2c___wasm_call_ctors(Z_script_instance_t*);
static u32 w2c_strcmp(Z_script_instance_t*, u32, u32);
static u32 w2c_check_flag(Z_script_instance_t*);
static void w2c_copy_char(Z_script_instance_t*, u32, u32);

static void init_globals(Z_script_instance_t* instance) {
  instance->w2c_g0 = 66864u;
  instance->w2c_input = 1072u;
  instance->w2c___dso_handle = 1024u;
  instance->w2c___data_end = 1328u;
  instance->w2c___global_base = 1024u;
  instance->w2c___heap_base = 66864u;
  instance->w2c___memory_base = 0u;
  instance->w2c___table_base = 1u;
}


static const u8 data_segment_data_w2c_d0[] = {
  0x78, 0x61, 0x6b, 0x67, 0x4b, 0x5c, 0x4e, 0x73, 0x28, 0x28, 0x6a, 0x3a, 
  0x6c, 0x39, 0x3c, 0x6d, 0x69, 0x6d, 0x6b, 0x3f, 0x3a, 0x6b, 0x3b, 0x39, 
  0x3b, 0x38, 0x3d, 0x38, 0x3f, 0x3d, 0x30, 0x3f, 0x3e, 0x6a, 0x6e, 0x6e, 
  0x3a, 0x6a, 0x3d, 0x6c, 0x75, 0x00, 0x00, 
};

static void init_memories(Z_script_instance_t* instance) {
  wasm_rt_allocate_memory(&instance->w2c_memory, 2, 65536);
  LOAD_DATA(instance->w2c_memory, 1024u, data_segment_data_w2c_d0, 43);
}

static void init_data_instances(Z_script_instance_t *instance) {
}

static void init_tables(Z_script_instance_t* instance) {
  wasm_rt_allocate_funcref_table(&instance->w2c_T0, 1, 1);
}

static void w2c___wasm_call_ctors(Z_script_instance_t* instance) {
  FUNC_PROLOGUE;
  FUNC_EPILOGUE;
}

static u32 w2c_strcmp(Z_script_instance_t* instance, u32 w2c_p0, u32 w2c_p1) {
  u32 w2c_l2 = 0, w2c_l3 = 0, w2c_l4 = 0, w2c_l5 = 0, w2c_l6 = 0, w2c_l7 = 0, w2c_l8 = 0, w2c_l9 = 0, 
      w2c_l10 = 0, w2c_l11 = 0, w2c_l12 = 0, w2c_l13 = 0, w2c_l14 = 0, w2c_l15 = 0, w2c_l16 = 0, w2c_l17 = 0, 
      w2c_l18 = 0, w2c_l19 = 0, w2c_l20 = 0, w2c_l21 = 0, w2c_l22 = 0, w2c_l23 = 0, w2c_l24 = 0, w2c_l25 = 0, 
      w2c_l26 = 0, w2c_l27 = 0, w2c_l28 = 0, w2c_l29 = 0, w2c_l30 = 0, w2c_l31 = 0, w2c_l32 = 0, w2c_l33 = 0, 
      w2c_l34 = 0, w2c_l35 = 0, w2c_l36 = 0, w2c_l37 = 0, w2c_l38 = 0, w2c_l39 = 0, w2c_l40 = 0, w2c_l41 = 0, 
      w2c_l42 = 0, w2c_l43 = 0;
  FUNC_PROLOGUE;
  u32 w2c_i0, w2c_i1;
  w2c_i0 = instance->w2c_g0;
  w2c_l2 = w2c_i0;
  w2c_i0 = 32u;
  w2c_l3 = w2c_i0;
  w2c_i0 = w2c_l2;
  w2c_i1 = w2c_l3;
  w2c_i0 -= w2c_i1;
  w2c_l4 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_p0;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 24, w2c_i1);
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_p1;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 20, w2c_i1);
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 24u);
  w2c_l5 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_l5;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 16, w2c_i1);
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 20u);
  w2c_l6 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_l6;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 12, w2c_i1);
  w2c_L1: 
    w2c_i0 = w2c_l4;
    w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 16u);
    w2c_l7 = w2c_i0;
    w2c_i0 = 1u;
    w2c_l8 = w2c_i0;
    w2c_i0 = w2c_l7;
    w2c_i1 = w2c_l8;
    w2c_i0 += w2c_i1;
    w2c_l9 = w2c_i0;
    w2c_i0 = w2c_l4;
    w2c_i1 = w2c_l9;
    i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 16, w2c_i1);
    w2c_i0 = w2c_l7;
    w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0));
    w2c_l10 = w2c_i0;
    w2c_i0 = w2c_l4;
    w2c_i1 = w2c_l10;
    i32_store8(&instance->w2c_memory, (u64)(w2c_i0) + 11, w2c_i1);
    w2c_i0 = w2c_l4;
    w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 12u);
    w2c_l11 = w2c_i0;
    w2c_i0 = 1u;
    w2c_l12 = w2c_i0;
    w2c_i0 = w2c_l11;
    w2c_i1 = w2c_l12;
    w2c_i0 += w2c_i1;
    w2c_l13 = w2c_i0;
    w2c_i0 = w2c_l4;
    w2c_i1 = w2c_l13;
    i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 12, w2c_i1);
    w2c_i0 = w2c_l11;
    w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0));
    w2c_l14 = w2c_i0;
    w2c_i0 = w2c_l4;
    w2c_i1 = w2c_l14;
    i32_store8(&instance->w2c_memory, (u64)(w2c_i0) + 10, w2c_i1);
    w2c_i0 = w2c_l4;
    w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0) + 11u);
    w2c_l15 = w2c_i0;
    w2c_i0 = 255u;
    w2c_l16 = w2c_i0;
    w2c_i0 = w2c_l15;
    w2c_i1 = w2c_l16;
    w2c_i0 &= w2c_i1;
    w2c_l17 = w2c_i0;
    w2c_i0 = w2c_l17;
    if (w2c_i0) {goto w2c_B2;}
    w2c_i0 = w2c_l4;
    w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0) + 11u);
    w2c_l18 = w2c_i0;
    w2c_i0 = 255u;
    w2c_l19 = w2c_i0;
    w2c_i0 = w2c_l18;
    w2c_i1 = w2c_l19;
    w2c_i0 &= w2c_i1;
    w2c_l20 = w2c_i0;
    w2c_i0 = w2c_l4;
    w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0) + 10u);
    w2c_l21 = w2c_i0;
    w2c_i0 = 255u;
    w2c_l22 = w2c_i0;
    w2c_i0 = w2c_l21;
    w2c_i1 = w2c_l22;
    w2c_i0 &= w2c_i1;
    w2c_l23 = w2c_i0;
    w2c_i0 = w2c_l20;
    w2c_i1 = w2c_l23;
    w2c_i0 -= w2c_i1;
    w2c_l24 = w2c_i0;
    w2c_i0 = w2c_l4;
    w2c_i1 = w2c_l24;
    i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 28, w2c_i1);
    goto w2c_B0;
    w2c_B2:;
    w2c_i0 = w2c_l4;
    w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0) + 11u);
    w2c_l25 = w2c_i0;
    w2c_i0 = 255u;
    w2c_l26 = w2c_i0;
    w2c_i0 = w2c_l25;
    w2c_i1 = w2c_l26;
    w2c_i0 &= w2c_i1;
    w2c_l27 = w2c_i0;
    w2c_i0 = w2c_l4;
    w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0) + 10u);
    w2c_l28 = w2c_i0;
    w2c_i0 = 255u;
    w2c_l29 = w2c_i0;
    w2c_i0 = w2c_l28;
    w2c_i1 = w2c_l29;
    w2c_i0 &= w2c_i1;
    w2c_l30 = w2c_i0;
    w2c_i0 = w2c_l27;
    w2c_l31 = w2c_i0;
    w2c_i0 = w2c_l30;
    w2c_l32 = w2c_i0;
    w2c_i0 = w2c_l31;
    w2c_i1 = w2c_l32;
    w2c_i0 = w2c_i0 == w2c_i1;
    w2c_l33 = w2c_i0;
    w2c_i0 = 1u;
    w2c_l34 = w2c_i0;
    w2c_i0 = w2c_l33;
    w2c_i1 = w2c_l34;
    w2c_i0 &= w2c_i1;
    w2c_l35 = w2c_i0;
    w2c_i0 = w2c_l35;
    if (w2c_i0) {goto w2c_L1;}
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0) + 11u);
  w2c_l36 = w2c_i0;
  w2c_i0 = 255u;
  w2c_l37 = w2c_i0;
  w2c_i0 = w2c_l36;
  w2c_i1 = w2c_l37;
  w2c_i0 &= w2c_i1;
  w2c_l38 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load8_u(&instance->w2c_memory, (u64)(w2c_i0) + 10u);
  w2c_l39 = w2c_i0;
  w2c_i0 = 255u;
  w2c_l40 = w2c_i0;
  w2c_i0 = w2c_l39;
  w2c_i1 = w2c_l40;
  w2c_i0 &= w2c_i1;
  w2c_l41 = w2c_i0;
  w2c_i0 = w2c_l38;
  w2c_i1 = w2c_l41;
  w2c_i0 -= w2c_i1;
  w2c_l42 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_l42;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 28, w2c_i1);
  w2c_B0:;
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 28u);
  w2c_l43 = w2c_i0;
  w2c_i0 = w2c_l43;
  goto w2c_Bfunc;
  w2c_Bfunc:;
  FUNC_EPILOGUE;
  return w2c_i0;
}

static u32 w2c_check_flag(Z_script_instance_t* instance) {
  u32 w2c_l0 = 0, w2c_l1 = 0, w2c_l2 = 0, w2c_l3 = 0, w2c_l4 = 0, w2c_l5 = 0, w2c_l6 = 0, w2c_l7 = 0, 
      w2c_l8 = 0, w2c_l9 = 0, w2c_l10 = 0;
  FUNC_PROLOGUE;
  u32 w2c_i0, w2c_i1;
  w2c_i0 = 0u;
  w2c_l0 = w2c_i0;
  w2c_i0 = 1072u;
  w2c_l1 = w2c_i0;
  w2c_i0 = 1024u;
  w2c_l2 = w2c_i0;
  w2c_i0 = w2c_l2;
  w2c_i1 = w2c_l1;
  w2c_i0 = w2c_strcmp(instance, w2c_i0, w2c_i1);
  w2c_l3 = w2c_i0;
  w2c_i0 = w2c_l3;
  w2c_l4 = w2c_i0;
  w2c_i0 = w2c_l0;
  w2c_l5 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_l5;
  w2c_i0 = w2c_i0 != w2c_i1;
  w2c_l6 = w2c_i0;
  w2c_i0 = 4294967295u;
  w2c_l7 = w2c_i0;
  w2c_i0 = w2c_l6;
  w2c_i1 = w2c_l7;
  w2c_i0 ^= w2c_i1;
  w2c_l8 = w2c_i0;
  w2c_i0 = 1u;
  w2c_l9 = w2c_i0;
  w2c_i0 = w2c_l8;
  w2c_i1 = w2c_l9;
  w2c_i0 &= w2c_i1;
  w2c_l10 = w2c_i0;
  w2c_i0 = w2c_l10;
  goto w2c_Bfunc;
  w2c_Bfunc:;
  FUNC_EPILOGUE;
  return w2c_i0;
}

static void w2c_copy_char(Z_script_instance_t* instance, u32 w2c_p0, u32 w2c_p1) {
  u32 w2c_l2 = 0, w2c_l3 = 0, w2c_l4 = 0, w2c_l5 = 0, w2c_l6 = 0, w2c_l7 = 0, w2c_l8 = 0, w2c_l9 = 0, 
      w2c_l10 = 0;
  FUNC_PROLOGUE;
  u32 w2c_i0, w2c_i1;
  w2c_i0 = instance->w2c_g0;
  w2c_l2 = w2c_i0;
  w2c_i0 = 16u;
  w2c_l3 = w2c_i0;
  w2c_i0 = w2c_l2;
  w2c_i1 = w2c_l3;
  w2c_i0 -= w2c_i1;
  w2c_l4 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_p0;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 12, w2c_i1);
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_p1;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 8, w2c_i1);
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 12u);
  w2c_l5 = w2c_i0;
  w2c_i0 = w2c_l5;
  w2c_i0 = !(w2c_i0);
  if (w2c_i0) {goto w2c_B0;}
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 12u);
  w2c_l6 = w2c_i0;
  w2c_i0 = 8u;
  w2c_l7 = w2c_i0;
  w2c_i0 = w2c_l6;
  w2c_i1 = w2c_l7;
  w2c_i0 ^= w2c_i1;
  w2c_l8 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i1 = w2c_l8;
  i32_store(&instance->w2c_memory, (u64)(w2c_i0) + 12, w2c_i1);
  w2c_B0:;
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 12u);
  w2c_l9 = w2c_i0;
  w2c_i0 = w2c_l4;
  w2c_i0 = i32_load(&instance->w2c_memory, (u64)(w2c_i0) + 8u);
  w2c_l10 = w2c_i0;
  w2c_i0 = w2c_l10;
  w2c_i1 = w2c_l9;
  i32_store8(&instance->w2c_memory, (u64)(w2c_i0) + 1072, w2c_i1);
  goto w2c_Bfunc;
  w2c_Bfunc:;
  FUNC_EPILOGUE;
}

/* export: 'memory' */
wasm_rt_memory_t* Z_scriptZ_memory(Z_script_instance_t* instance) {
  return &instance->w2c_memory;
}

/* export: '__wasm_call_ctors' */
void Z_scriptZ___wasm_call_ctors(Z_script_instance_t* instance) {
  return w2c___wasm_call_ctors(instance);
}

/* export: 'strcmp' */
u32 Z_scriptZ_strcmp(Z_script_instance_t* instance, u32 w2c_p0, u32 w2c_p1) {
  return w2c_strcmp(instance, w2c_p0, w2c_p1);
}

/* export: 'check_flag' */
u32 Z_scriptZ_check_flag(Z_script_instance_t* instance) {
  return w2c_check_flag(instance);
}

/* export: 'input' */
u32* Z_scriptZ_input(Z_script_instance_t* instance) {
  return &instance->w2c_input;
}

/* export: 'copy_char' */
void Z_scriptZ_copy_char(Z_script_instance_t* instance, u32 w2c_p0, u32 w2c_p1) {
  return w2c_copy_char(instance, w2c_p0, w2c_p1);
}

/* export: '__dso_handle' */
u32* Z_scriptZ___dso_handle(Z_script_instance_t* instance) {
  return &instance->w2c___dso_handle;
}

/* export: '__data_end' */
u32* Z_scriptZ___data_end(Z_script_instance_t* instance) {
  return &instance->w2c___data_end;
}

/* export: '__global_base' */
u32* Z_scriptZ___global_base(Z_script_instance_t* instance) {
  return &instance->w2c___global_base;
}

/* export: '__heap_base' */
u32* Z_scriptZ___heap_base(Z_script_instance_t* instance) {
  return &instance->w2c___heap_base;
}

/* export: '__memory_base' */
u32* Z_scriptZ___memory_base(Z_script_instance_t* instance) {
  return &instance->w2c___memory_base;
}

/* export: '__table_base' */
u32* Z_scriptZ___table_base(Z_script_instance_t* instance) {
  return &instance->w2c___table_base;
}

void Z_script_init_module(void) {
  assert(wasm_rt_is_initialized());
  s_module_initialized = true;
  init_func_types();
}

void Z_script_instantiate(Z_script_instance_t* instance) {
  assert(wasm_rt_is_initialized());
  assert(s_module_initialized);
  init_globals(instance);
  init_memories(instance);
  init_tables(instance);
  init_data_instances(instance);
}

void Z_script_free(Z_script_instance_t* instance) {
  wasm_rt_free_funcref_table(&instance->w2c_T0);
  wasm_rt_free_memory(&instance->w2c_memory);
}
